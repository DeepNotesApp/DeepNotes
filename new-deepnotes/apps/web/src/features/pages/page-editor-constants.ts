/** Legacy plain `Y.Text` name from older DeepNotes builds (one-time migration). */
export const Y_TEXT_DEFAULT = "default";

/** ProseMirror rich-text field on the shared Yjs doc. */
export const Y_FRAG_PROSEMIRROR = "prosemirror";
