/** CSS pixels from the top of a container note to the start of its content area.
 *  Must match the `top: 3rem` offset in DisplayNote.vue container children div. */
export const CONTAINER_CONTENT_OFFSET_Y = 48;
