{
  "extends": "tsconfig/base.json",

  "include": ["*.ts", "src/**/*.ts"],

  "compilerOptions": {
    "baseUrl": ".",

    "rootDir": ".",

    "typeRoots": ["node_modules/@types"] // Needed for tests
  }
}
